import makeAddComment from './add-comment.js';
import makeListComments from './list-commnets.js';
// import db from '../data-access/index.js';

// const addComment = makeAddComment(db);

export default Object.freeze({
  makeAddComment,
  makeListComments,
});
